"use strict";
describe("Issue Service: ", () => {
    let issueService;
    /*beforeEach(() => {
      TestBed.configureTestingModule({
        declarations: [IssuesService]
      });
  
      issueService = TestBed.createComponent(IssuesService).componentInstance;
    })
  
    describe("GetUrl" , () => {
  
      it("Should return last page +1", () => { expect(issueService.getUrl(2)).toContain("page=3")})
  
    })*/
});
//# sourceMappingURL=issues.service.spec.js.map